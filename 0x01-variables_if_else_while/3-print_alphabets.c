#include <stdio.h>


/** 
 * main - print alphabet in lower and upper
 *
 * Return: zero
 */
int main(void)
{
	int ch;
	int CH;

	for (cha = 'a'; ch <= 'z'; ch++)
		putchar(ch);
	for (CH = 'A'; CH <= 'Z'; CH++)
		putchar(CH);
	putchar('\n');
	return (0);
}
